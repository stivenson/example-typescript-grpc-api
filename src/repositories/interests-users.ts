import { BaseRepository } from '@repositories/base/BaseRepository';
import InterestsUsers from '@models/interests-users';

export class InterestsUsersRepository extends BaseRepository<InterestsUsers> {
    // custom methods
}
